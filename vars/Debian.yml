---
dependencies:
 - libaugeas0
 - cron
 - acl  # this is required to set permissions on temp files

apache_service: apache2
apache_daemon: apache2
apache_daemon_path: /usr/sbin/
apache_conf_path: /etc/apache2/sites-enabled

cron_service: cron
